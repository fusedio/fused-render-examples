# Optional — only needed for the hosted-model nodes (HF Text to Image, HF
# Image Caption, Seedance Text to Video). Everything else works offline.
HF_TOKEN=your-huggingface-token       # huggingface.co/settings/tokens
ARK_API_KEY=your-byteplus-modelark-key  # BytePlus ModelArk console
